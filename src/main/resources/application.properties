# Server Configuration
server.port=8080
server.servlet.context-path=/api

# Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/skill2?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=Vi@130704
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.jdbc.time_zone=UTC
spring.jackson.time-zone=Asia/Kolkata

# JWT Configuration
jwt.secret=5367566B59703373367639792F423F4528482B4D6251655468576D5A71347437
jwt.expiration=86400000

# File Upload Configuration
spring.servlet.multipart.enabled=true

# Async Configuration
spring.task.execution.pool.core-size=5
spring.task.execution.pool.max-size=10
spring.task.execution.pool.queue-capacity=100

# CORS Configuration
cors.allowed.origins=http://localhost:3000,http://localhost:3001


# Logging
logging.level.org.springframework.security=DEBUG
logging.level.com.skillverse=DEBUG

logging.level.org.springframework.web.socket=DEBUG
logging.level.org.springframework.messaging=DEBUG

spring.jpa.hibernate.naming.physical-strategy=org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl

# Add these configurations to your application.properties

# WebSocket Configuration
websocket.url=ws://localhost:8080/ws/signaling

# Media Server Configuration (Mediasoup)
media.server.url=http://localhost:3000

# TURN Server Configuration (for production)
turn.server.url=turn:your-turn-server.com:3478
turn.server.username=turnuser
turn.server.password=turnpassword

# Recording Storage Path
recording.storage.path=/var/skillverse/recordings
recording.base.url=http://localhost/recordings

# Maximum file upload size (for future use)
spring.servlet.multipart.max-file-size=500MB
spring.servlet.multipart.max-request-size=500MB