############################################
# 🌐 Server Configuration
############################################
server.port=${PORT:8080}
server.servlet.context-path=/api

############################################
# 💾 Database Configuration
############################################
spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3306/skill2?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true}
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=${DB_PASSWORD:Vi@130704}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

############################################
# 🧩 JPA Configuration
############################################
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.jdbc.time_zone=UTC
spring.jackson.time-zone=Asia/Kolkata
spring.jpa.hibernate.naming.physical-strategy=org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl

############################################
# 🔐 JWT Configuration
############################################
jwt.secret=${JWT_SECRET:5367566B59703373367639792F423F4528482B4D6251655468576D5A71347437}
jwt.expiration=${JWT_EXPIRATION:86400000}

############################################
# 📦 File Upload Configuration
############################################
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=${MAX_FILE_SIZE:500MB}
spring.servlet.multipart.max-request-size=${MAX_REQUEST_SIZE:500MB}

############################################
# ⚙️ Async Configuration
############################################
spring.task.execution.pool.core-size=5
spring.task.execution.pool.max-size=10
spring.task.execution.pool.queue-capacity=100

############################################
# 🔄 CORS Configuration
############################################
# Local fallback: localhost ports
cors.allowed.origins=${CORS_ALLOWED_ORIGINS:http://localhost:3000,http://localhost:3001}

############################################
# 🪵 Logging Configuration
############################################
logging.level.org.springframework.security=DEBUG
logging.level.com.skillverse=DEBUG
logging.level.org.springframework.web.socket=DEBUG
logging.level.org.springframework.messaging=DEBUG

############################################
# 📡 WebSocket Configuration
############################################
# Local fallback uses ws://, production uses wss://
websocket.url=${WEBSOCKET_URL:ws://localhost:8080/ws/signaling}

############################################
# 🎥 Media Server (Mediasoup)
############################################
media.server.url=${MEDIA_SERVER_URL:http://localhost:3000}

############################################
# 🌍 TURN Server (optional for production)
############################################
turn.server.url=${TURN_SERVER_URL:turn:your-turn-server.com:3478}
turn.server.username=${TURN_SERVER_USERNAME:turnuser}
turn.server.password=${TURN_SERVER_PASSWORD:turnpassword}

############################################
# 💾 Recording Configuration
############################################
recording.storage.path=${RECORDING_STORAGE_PATH:/var/skillverse/recordings}
recording.base.url=${RECORDING_BASE_URL:http://localhost/recordings}
